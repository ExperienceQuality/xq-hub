# Satellite catalogue lives in docs/satellites.md

Agents need one place for Satellite name, link, purpose, and the matching Ticket label. We keep that as Hub markdown `docs/satellites.md`; `satellite:<name>` labels on Tickets mirror the catalogue. We rejected labels-only (no purpose/link prose), embedding the inventory in `CONTEXT.md` (glossary vs changing inventory), and org-API auto-generation day one (tooling tax; still weak on purpose lines).
