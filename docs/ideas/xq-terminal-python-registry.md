# Idea: xq-terminal Python registry

**Status:** Collapsed to Specs — see [`docs/specs/xq-terminal-registry.md`](../specs/xq-terminal-registry.md) and [`docs/specs/xq-terminal.md`](../specs/xq-terminal.md) (**Python-only pivot**).

YAML sanitize → generated registry `pyproject.toml` dependencies → Terminal imports `xq-terminal-registry` only.
