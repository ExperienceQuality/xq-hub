# Idea: xq-motest-cli

**Status:** Cancelled — an existing project already covers the same goal (agent-native mobile testing). Do not reinvent; do not slice Tickets from this Idea.

Agent-native CLI so agents can test on mobile (Vibium-style map → `@refs` → act, iOS Simulator first) was explored and collapsed to a Spec, then abandoned in favour of adopting the other project.
